apiVersion: v1
kind: Pod
metadata:
  name: pod-config
  namespace: roboshop

spec:
  containers:
    - name: nginx
      image: nginx
      envFrom:
        - configMapKeyRef:
            name: configmap-demo
           